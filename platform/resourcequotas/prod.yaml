apiVersion: v1
kind: ResourceQuota
metadata:
  name: cloudops-prod-quota
  namespace: cloudops-prod
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "8"
    limits.memory: 8Gi
    pods: "25"
    services: "10"
