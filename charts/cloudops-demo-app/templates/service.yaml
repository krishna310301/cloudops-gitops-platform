apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudops-demo-app.fullname" . }}
  labels:
    {{- include "cloudops-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudops-demo-app.selectorLabels" . | nindent 4 }}
