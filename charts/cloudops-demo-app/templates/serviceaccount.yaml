{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cloudops-demo-app.serviceAccountName" . }}
  labels:
    {{- include "cloudops-demo-app.labels" . | nindent 4 }}
{{- end }}
